# CuliFeed Production Dependencies
# Core packages required for production runtime
# Updated for Python 3.13 compatibility with latest stable versions

# Web and HTTP
requests>=2.32.0               # HTTP client for API calls and feed fetching
aiohttp>=3.10.0                # Async HTTP client for concurrent operations

# RSS and Content Processing
feedparser>=6.0.11             # RSS/Atom feed parsing
beautifulsoup4>=4.12.3         # HTML parsing and content extraction

# Telegram Bot
python-telegram-bot>=21.0      # Telegram bot API wrapper (latest stable)

# AI and Machine Learning (All Used)
google-generativeai>=0.8.0     # Google Gemini API client
openai>=1.50.0                 # OpenAI API client (used for OpenAI and DeepSeek providers)
groq>=0.12.0                   # Groq API client

# Data Validation and Configuration
pydantic>=2.9.0                # Data validation and settings management
pydantic-settings>=2.6.0       # Settings management with Pydantic
PyYAML>=6.0.2                  # YAML configuration file support
python-dotenv>=1.0.0           # Environment variable loading from .env files

# Utilities and Tools
python-dateutil>=2.9.0         # Enhanced date parsing
pytz>=2024.2                   # Timezone support (latest)
schedule>=1.2.2                # Job scheduling for daily processing

# CLI Interface (Essential for main.py management commands)
click>=8.1.7                   # CLI interface - needed for main.py --init-db, --health-check
rich>=13.9.0                   # Rich console output for CLI

# Production deployment
supervisor>=4.2.5              # Process management (Python version - no system package needed)

# Vector search
sqlite-vec>=0.1.0              # SQLite vector extension for embedding similarity search
