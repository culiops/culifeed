name: Build and Push Docker Image

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Docker tag to build and push'
        required: true
        default: 'manual'
        type: string

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        variant:
          - name: "debian"
            dockerfile: "Dockerfile"
            suffix: ""
          - name: "alpine"
            dockerfile: "Dockerfile.alpine"
            suffix: "-alpine"

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=semver,pattern={{version}}${{ matrix.variant.suffix }},enable=${{ github.event_name == 'release' }}
          type=semver,pattern={{major}}.{{minor}}${{ matrix.variant.suffix }},enable=${{ github.event_name == 'release' }}
          type=raw,value=latest${{ matrix.variant.suffix }},enable=${{ github.event_name == 'release' }}
          type=raw,value=${{ github.event.inputs.tag }}${{ matrix.variant.suffix }},enable=${{ github.event_name == 'workflow_dispatch' }}

    - name: Build Docker image for testing (${{ matrix.variant.name }})
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ${{ matrix.variant.dockerfile }}
        platforms: linux/amd64
        push: false
        load: true
        tags: culifeed-test:${{ matrix.variant.name }}
        cache-from: type=gha,key=${{ matrix.variant.name }}
        cache-to: type=gha,mode=max,key=${{ matrix.variant.name }}

    - name: Test Docker image (${{ matrix.variant.name }})
      run: |
        echo "🧪 Testing ${{ matrix.variant.name }} image before push..."

        # Test 1: Dependencies verification
        echo "📋 Test 1: Dependencies verification"
        docker run --rm --entrypoint="" culifeed-test:${{ matrix.variant.name }} \
          python -c "
        import asyncio, aiohttp, feedparser, telegram, yaml
        from bs4 import BeautifulSoup
        print('✅ All critical dependencies available')
        "

        # Test 2: Configuration loading
        echo "📋 Test 2: Configuration loading"
        docker run --rm --entrypoint="" \
          -e CULIFEED_TELEGRAM__BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890_test" \
          -e CULIFEED_DATABASE__PATH="/tmp/test.db" \
          -e CULIFEED_AI__GROQ_API_KEY="test_groq_api_key_for_ci_testing" \
          -e CULIFEED_AI__GEMINI_API_KEY="test_gemini_api_key_for_ci_testing" \
          -e CULIFEED_AI__OPENAI_API_KEY="test_openai_api_key_for_ci_testing" \
          -e CULIFEED_AI__DEEPSEEK_API_KEY="test_deepseek_api_key_for_ci_testing" \
          culifeed-test:${{ matrix.variant.name }} \
          python -c "
        import sys
        sys.path.append('/app')
        from culifeed.config.settings import get_settings
        settings = get_settings()
        print('✅ Configuration loads successfully')
        print(f'Available AI providers: {len([p for p in [settings.ai.groq_api_key, settings.ai.gemini_api_key, settings.ai.openai_api_key, settings.ai.deepseek_api_key] if p])}')
        "

        # Test 3: Entry points exist
        echo "📋 Test 3: Service entry points"
        docker run --rm --entrypoint="" culifeed-test:${{ matrix.variant.name }} \
          sh -c 'test -f /app/run_bot.py && test -f /app/run_scheduler.py && echo "✅ Entry points exist"'

        # Test 4: Configuration validation
        echo "📋 Test 4: Configuration validation"
        docker run --rm --entrypoint="" \
          -e CULIFEED_TELEGRAM__BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890_test" \
          -e CULIFEED_DATABASE__PATH="/tmp/test.db" \
          -e CULIFEED_AI__GROQ_API_KEY="test_groq_api_key_for_ci_testing" \
          -e CULIFEED_AI__GEMINI_API_KEY="test_gemini_api_key_for_ci_testing" \
          -e CULIFEED_AI__OPENAI_API_KEY="test_openai_api_key_for_ci_testing" \
          -e CULIFEED_AI__DEEPSEEK_API_KEY="test_deepseek_api_key_for_ci_testing" \
          culifeed-test:${{ matrix.variant.name }} \
          python main.py check-config

        echo "✅ All tests passed for ${{ matrix.variant.name }}!"

    - name: Build and push Docker image (${{ matrix.variant.name }})
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ${{ matrix.variant.dockerfile }}
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha,key=${{ matrix.variant.name }}

    - name: Cleanup test image (${{ matrix.variant.name }})
      if: always()
      run: docker rmi culifeed-test:${{ matrix.variant.name }} || true